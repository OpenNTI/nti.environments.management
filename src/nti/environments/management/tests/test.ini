[general]
devmode = false

[celery]
celery.broker_url = memory://
celery.backend_url = cache+memory://

[dns]
base_domain=test_ntdomain.com
zone=nextthot.com.
alias_target=spin.nextthot.com

[haproxy]
config_root = /tmp/haproxy/etc/
admin_socket = /run/haproxy-master.sock

[pods]
root_dir=/tmp/pods
pod_logs_dir=logs
